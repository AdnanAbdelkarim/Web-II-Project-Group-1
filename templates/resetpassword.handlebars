<h1>Reset Password</h1>
 {{#if errorMessage}}
    <div class="alert alert-danger" role="alert">
        {{errorMessage}}
    </div>

 {{/if}}
<form method="post">
    <label for="email">Email</label><br>
    <input id='email' name="email" class="form-control"><br>
    <input type="submit" value="Send Link" class="btn btn-primary mt-2">
</form>